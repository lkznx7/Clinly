UserRole:
  type: string
  enum:
    - USER
    - ADMIN
  description: >
    Defines the role of a user in the Clinly platform.
    - `USER` — regular platform user
    - `ADMIN` — administrator with elevated privileges
  example: USER
