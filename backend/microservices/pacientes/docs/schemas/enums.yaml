# =============================================================================
# PATIENT ENUMS — Clinly API
# =============================================================================

PatientStatus:
  description: Status do paciente no sistema
  type: string
  enum:
    - ACTIVE
    - INACTIVE
    - ARCHIVED

Gender:
  description: Gênero do paciente
  type: string
  enum:
    - MALE
    - FEMALE
    - OTHER
